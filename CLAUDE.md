@AGENTS.md

## Claude Code

The repo-wide guidance above is imported from `AGENTS.md`, which is shared with
other agents like Codex — keep changes there, not here. Use this section only for
Claude-Code-specific notes.

- When working under `frontend/`, `frontend/CLAUDE.md` also applies — it loads
  automatically once you start reading files in that directory.
- Remember the one hard rule from `AGENTS.md`: **don't author the Go backend**
  in `backend/`.
